# <项目名> 架构地图

## 项目概述

<项目一句话描述>

## 模块分层

### 依赖方向（严格单向）

```
types → config → repo → service → runtime → api
  ↑       ↑       ↑       ↑          ↑        ↑
纯类型  配置加载  数据访问  业务逻辑   启动管理  对外接口
```

**规则：**
- 每层只能依赖左侧的层
- 禁止循环依赖
- `providers/` 是横切关注点的唯一入口（认证、日志、遥测、Feature Flag）

## 目录结构

```
<根据语言填写>
```

## 主要业务域

> 初始为空，随项目发展补充

## 外部依赖

> 列出主要依赖及其作用，更新于每次引入新依赖时

## 质量评分

| 模块 | 覆盖率 | 文档完整度 | 最后更新 |
|------|--------|------------|---------|
| （待填写） | - | - | - |
